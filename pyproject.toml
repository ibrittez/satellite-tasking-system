[project]
name = "sat_task_system"
version = "0.1.0"
dependencies = []

[project.optional-dependencies]
dev = ["pytest"]

[tool.pytest.ini_options]
addopts = "-v"
pythonpath = ["src"]

[tool.pyright]
extraPaths = ["src"]
